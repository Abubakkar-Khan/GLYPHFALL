import type { Metadata } from "next";
import { Geist_Mono } from "next/font/google";
import "./globals.css";

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GLYPHFALL — ASCII Physics Sandbox",
  description: "Real-time ASCII physics sandbox powered by modern web technologies. Drop sand, water, fire, and bombs in a cellular automata simulation rendered with Unicode glyphs.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistMono.variable} h-full`}>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
